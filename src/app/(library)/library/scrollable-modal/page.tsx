import ScrollableModal from "@/app/(library)/_modules/components/ScrollableModal";

const ScrollableModalPage = () => {
  return (
    <>
      <ScrollableModal />
    </>
  );
};

export default ScrollableModalPage;
